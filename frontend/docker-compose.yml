version: '3.8'

services:
  rednote-assistent:
    build: .
    container_name: rednote-assistent
    ports:
      - "80:80"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
